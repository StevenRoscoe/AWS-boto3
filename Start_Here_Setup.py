#Before writing your code, you need to install boto3 and the AWSCLI. Use the following commands below in the terminal:
pip3 install awscli
pip3 install boto3

#Setup your IAM profile if you are using another account. I'll be using the same account so I don't have to.
#When in the users tab give administrator access to the profile.
#Afterwards, configure the AWSCLI with your credentials.