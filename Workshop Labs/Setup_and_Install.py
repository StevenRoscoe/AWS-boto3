#In the command prompt, type in the following command to install Python for AWS:
pip3 install boto3

#To upgrade the pip version:
pip install --upgrade pip

#To see what packages you have installed:
pip freeze

#Install the pip requirements text file in the command prompt:
pip freeze > requirements.txt

#To install it in a different environment:
pip install -r requirements.txt